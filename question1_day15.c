//Write a program to calculate the factorial of a number
#include <stdio.h>

int main() {
    int n;
    int factorial = 1;
    printf("Enter the number: ");
    scanf("%d", &n);

    return 0;
}