#include <stdio.h>
int main() {
    int costprice,sellingprice,amount,profit,loss,profit_percnetage,loss_percentage;

    printf("Enter the cost price:");
    scanf("%d",&costprice);

    printf("Enter the selling price:");
    scanf("%d",&sellingprice);

    if(sellingprice > costprice) {
        p
    }
}